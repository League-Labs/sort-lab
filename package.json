{
  "name": "sort-lab-demo",
  "version": "1.0.0",
  "description": "Demo for a sort algorithm visualization in TypeScript.",
  "scripts": {
    "dev": "npx serve . -l 9000",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -f SortDemo.js"
  },
  "devDependencies": {
    "serve": "^14.2.0",
    "typescript": "^5.8.3"
  }
}
