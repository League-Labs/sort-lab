{
  "name": "sort-lab-demo",
  "version": "1.0.0",
  "description": "Demo for a sort algorithm visualization in TypeScript.",
  "scripts": {
    "dev": "npx serve . -l 9000",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "clean": "rm -f SortDemo.js",
    "dev:watch": "concurrently \"npm run build:watch\" \"npx serve . -l 9000\""
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "serve": "^14.2.0",
    "typescript": "^5.8.3"
  }
}
