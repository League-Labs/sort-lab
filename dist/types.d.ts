/**
 * Represents the different states a bar can be in during sorting
 */
export declare enum BarState {
    UNSORTED = "unsorted",
    SORTED = "sorted",
    PIVOT = "pivot",
    COMPARING = "comparing",
    SWAPPING = "swapping"
}
